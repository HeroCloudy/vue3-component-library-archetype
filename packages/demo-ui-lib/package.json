{
  "name": "@{{libraryName}}/{{libraryName}}",
  "version": "1.0.0",
  "description": "组件库",
  "main": "index.ts",
  "scripts": {
    "build": "rimraf ../../lib && vue-tsc --noEmit && vite build"
  },
  "keywords": [],
  "author": "{{author}}",
  "license": "ISC",
  "dependencies": {
    "@{{libraryName}}/foo": "workspace:^1.0.0"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "vite-plugin-dts": "^2.3.0"
  }
}
